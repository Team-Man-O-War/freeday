var React = require('react');

var Pins = React.createClass({//Individual event items that will appear on Map component based on filtering. Pulls from Events component.
	render: function(){
		return (

		);
	}
});

module.exports = Pins;